<?xml version="1.0" encoding="utf-8"?>

<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- This is the line -->
    <item>
        <shape>
            <padding android:top="0sp" android:left="10sp"
                android:bottom="0sp" android:right="10sp"/>
            <solid android:color="@string/secondary" />
        </shape>
    </item>
    <!-- This is the main color -->
    <item>
        <shape>
            <solid android:color="@string/backGround" />
        </shape>
    </item>
</layer-list>